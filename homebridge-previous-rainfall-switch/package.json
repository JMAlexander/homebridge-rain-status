{
  "name": "homebridge-previous-rainfall-switch",
  "version": "1.0.0",
  "description": "Homebridge plugin that toggles a switch based on previous 24-hour rainfall amounts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "homebridge-plugin",
    "rain",
    "weather",
    "switch"
  ],
  "author": "Jeff A.",
  "license": "MIT",
  "engines": {
    "homebridge": ">=1.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "peerDependencies": {
    "homebridge": ">=1.0.0",
    "hap-nodejs": ">=0.10.0"
  },
  "devDependencies": {
    "homebridge": ">=1.0.0",
    "hap-nodejs": ">=0.10.0"
  }
}
